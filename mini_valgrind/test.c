/**
* Mini Valgrind Lab
* CS 241 - Fall 2018
*/

#include <stdio.h>
#include <stdlib.h>

int main() {
    // int *num = malloc(sizeof(int));
    // int *arr = calloc(5, sizeof(int));
    // char *str = malloc(13);
    // free(arr);
    // arr = realloc(arr, 0);
    // free(num);
    // free(str);
    return 0;
}

